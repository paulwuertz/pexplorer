# Remark

Here the assumption for a wasm in-browser run is, that a sELFreport is already filled including DisAsm with capstone.js

On the x86 cli program we have to do so using the shared capstone library.
